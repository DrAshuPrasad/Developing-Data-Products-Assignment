# running shiny app
# copy ui.R and server.R in C:/mtcars directory
# invoke RStudio. Install shiny library, if needed. Run the following in RStudio:
	# library(shiny)
	# setwd("C:/mtcars/")
	# runApp(".")
